name: Artifact Demo

on: push

jobs:
 build:
    runs-on: ubuntu-latest

    steps:
      - name: create file
        run: |
         mkdir temp
         echo "moni" > temp/alpha.html

      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: files
          path: temp/
